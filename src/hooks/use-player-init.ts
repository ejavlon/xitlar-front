/**
 * Player init hook: Kept for hydration/lifecycle purposes.
 * By default, player starts empty until the user selects a track.
 */
export const usePlayerInit = () => {
  // No-op on initial mount: Player remains in empty idle state.
};
